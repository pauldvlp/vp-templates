# GitHub PAT used by the `github` MCP server in .mcp.json. Never hardcode it there —
# .mcp.json is committed, this file is not.
# Scopes: repo (read), read:org. Create at https://github.com/settings/tokens
GITHUB_PAT=
