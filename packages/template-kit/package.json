{
  "name": "@pauldvlp/template-kit",
  "version": "0.0.0",
  "private": true,
  "description": "Internal helpers + CLI runtime shared by the @pauldvlp generators (file-tree building, shadcn components.json, icon libs, catalog resolution, the runTemplateCLI runtime). Not published — bundled into each generator's dist via tsconfig paths.",
  "type": "module",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./testing": "./src/testing.ts"
  },
  "files": [
    "src"
  ],
  "dependencies": {
    "@clack/prompts": "^1.7.0"
  },
  "devDependencies": {
    "vitest": "^4"
  },
  "engines": {
    "node": ">=22.18.0"
  }
}
